# hr_ai_assistant
HR AI Assistant is a Streamlit-based app that answers employee queries using internal HR policy documents. It uses semantic search with embeddings to retrieve relevant content and optionally generates LLM-based responses. Ideal for hackathons and internal knowledge bots.
